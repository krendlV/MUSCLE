# requirements.txt

# Core Jupyter & Voila
jupyterlab>=3.0.0
voila>=0.3.0
ipywidgets>=7.6.0

# Scientific & Image Processing
numpy>=1.20.0
opencv-python-headless>=4.5.0 # Use headless unless GUI interaction is needed
scikit-image>=0.18.0
matplotlib>=3.4.0
tifffile>=2021.0.0
pandas>=1.3.0
openpyxl>=3.0.0 # For Excel output

# Cellpose & PyTorch (CPU version for broad compatibility)
# See: https://pytorch.org/get-started/locally/
torch>=1.9.0+cpu
torchvision>=0.10.0+cpu
cellpose>=2.0,<4.0 # Or specify a more precise version if needed

# Optional: natsort for potentially better folder sorting if needed later
# natsort
